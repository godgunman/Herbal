<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.edu.ntust.dt.herbal"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/Theme.MyAppTheme" >
        <activity
            android:name="tw.edu.ntust.dt.herbal.LoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" >
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.DashboardActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyAppTheme" >
            <!-- <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> -->
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.MeasureActvitiy"
            android:label="MEASURE"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyAppTheme" >
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.ResultActivity"
            android:label="RESULT"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyAppTheme" >
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.HistoryActivity"
            android:label="HISTORY"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyAppTheme" >
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.SymptomActivity"
            android:label="SYMPTOM"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyAppTheme" >
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.ShopActivity"
            android:label="HERBAL SHOP"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyAppTheme" >
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.HerbalDetailActivity"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Dialog" >
        </activity>
        <activity
            android:name="tw.edu.ntust.dt.herbal.ConfirmActivity"
            android:label="HERBAL SHOP"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:launchMode="singleTop"
            android:value="@string/facebook_app_id" />
    </application>

</manifest>